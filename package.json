{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm --workspace web run build",
    "lint": "npm --workspace web run lint"
  },
  "engines": {
    "node": ">=20"
  }
}
